import { View, Text } from "react-native";

export default function ReservasScreen() {
  return (
    <View className="flex-1 justify-center items-center bg-white">
      <Text className="text-xl text-gray-700">
        Lista de Reservas (Estático)
      </Text>
    </View>
  );
}
